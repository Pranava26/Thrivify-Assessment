{
  "name": "thrivify-assessment",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "npm install --prefix backend && npm install --prefix frontend && npm run build --prefix frontend && pip install -r flask_backend/requirements.txt",
    "start": "npm run start --prefix backend && gunicorn --chdir flask_backend run:app"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
